const persona={
    nombre:"Kakaroto",
    edad:25,
    casado:false,
    hijo:{
        nombre:"saitama",
    }
}
console.log(persona)
console.log(`nombre: ${persona.nombre}`)
console.log(`edad: ${persona.edad}`)
console.log(`casado: ${persona.casado}`)
console.log(`hojo: ${persona.hijo.nombre}`)