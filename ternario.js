const cuenta = -10;
const mensaje = cuenta < 0 ? "No hay saldo" : "tienes mucha plata";
const mensaj = cuenta > 0 && "kisamado"

// solo de hacer validaciones de una sola con && si es true if(true){return ......}
// if (cuenta < 0) mensaje = "no hay saldo";
// else mensaje = "tiene saldo";


document.write(mensaje);
document.write(mensaj);